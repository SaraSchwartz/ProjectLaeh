import { ValidPassDirective } from './valid-pass.directive';

describe('ValidPassDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidPassDirective();
    expect(directive).toBeTruthy();
  });
});
