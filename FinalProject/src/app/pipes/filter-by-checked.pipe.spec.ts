import { FilterByCheckedPipe } from './filter-by-checked.pipe';

describe('FilterByCheckedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCheckedPipe();
    expect(pipe).toBeTruthy();
  });
});
