import { FilterByScreenSettingsPipe } from './filter-by-screen-settings.pipe';

describe('FilterByScreenSettingsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByScreenSettingsPipe();
    expect(pipe).toBeTruthy();
  });
});
